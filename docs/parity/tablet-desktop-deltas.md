# Tablet / Desktop Deltas

- Bottom tab becomes side rail at `md+`.
- Content may switch from one column to two columns only when the same mobile blocks are being redistributed.
- Station list becomes a card grid.
- Station detail becomes summary/content two-column layout.
- No desktop-only metadata panel, overview panel, analytics block, or explanatory note was added.
- Account keeps the same root card stack and only widens spacing/columns where the same blocks already exist.
- Plans keep the same socket card, session card, and plan cards; desktop does not add a pricing summary sidebar.
